using '../main.bicep'

param baseName = 'sldbom'
param environment = 'prod'
param appServiceSku = 'P1v3'
// param azureOpenAiEndpoint = ''    // Set via deploy.sh or override here
// param azureDiEndpoint = ''        // Set via deploy.sh or override here
